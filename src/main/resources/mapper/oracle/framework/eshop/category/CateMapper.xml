<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.seeyoui.kensite.framework.eshop.category.persistence.CateMapper" >
	<!-- 查询单表的字段 -->
	<sql id="tableColumns">
		T.ID AS "id",
		T.CREATE_DATE AS "createDate",
		T.CREATE_USER AS "createUser",
		T.UPDATE_DATE AS "updateDate",
		T.UPDATE_USER AS "updateUser",
		T.REMARKS AS "remarks",
		T.DEL_FLAG AS "delFlag",
		T.NAME AS "name",
		T.KEY_WORDS AS "keyWords",
		T.DESCRIBE AS "describe",
		T.SEQ AS "seq",
		T.PARENT_ID AS "parentId",
		T.MEASURE_UNIT AS "measureUnit",
		T.GRADE AS "grade",
		T.IS_SHOW AS "isShow",
		T.SHOP_ID AS "shopId"
	</sql>
	<!-- 查询过滤条件 -->
	<sql id="whereStr">
		<where>
			1=1
			<if test="id != null and id != ''">
				
			</if>
			<if test="createDate != null and createDate != ''">
				AND T.CREATE_DATE = #{createDate}
			</if>
			<if test="createUser != null and createUser != ''">
				AND T.CREATE_USER LIKE '%'||#{createUser}||'%'
			</if>
			<if test="updateDate != null and updateDate != ''">
				AND T.UPDATE_DATE = #{updateDate}
			</if>
			<if test="updateUser != null and updateUser != ''">
				AND T.UPDATE_USER LIKE '%'||#{updateUser}||'%'
			</if>
			<if test="remarks != null and remarks != ''">
				AND T.REMARKS LIKE '%'||#{remarks}||'%'
			</if>
			<if test="delFlag != null and delFlag != ''">
				AND T.DEL_FLAG LIKE '%'||#{delFlag}||'%'
			</if>
			<if test="name != null and name != ''">
				AND T.NAME LIKE '%'||#{name}||'%'
			</if>
			<if test="keyWords != null and keyWords != ''">
				AND T.KEY_WORDS LIKE '%'||#{keyWords}||'%'
			</if>
			<if test="describe != null and describe != ''">
				AND T.DESCRIBE LIKE '%'||#{describe}||'%'
			</if>
			<if test="seq != null and seq != ''">
				AND T.SEQ = #{seq}
			</if>
			<if test="parentId != null and parentId != ''">
				AND T.PARENT_ID LIKE '%'||#{parentId}||'%'
			</if>
			<if test="measureUnit != null and measureUnit != ''">
				AND T.MEASURE_UNIT LIKE '%'||#{measureUnit}||'%'
			</if>
			<if test="grade != null and grade != ''">
				AND T.GRADE = #{grade}
			</if>
			<if test="isShow != null and isShow != ''">
				AND T.IS_SHOW LIKE '%'||#{isShow}||'%'
			</if>
			<if test="shopId != null and shopId != ''">
				AND T.SHOP_ID LIKE '%'||#{shopId}||'%'
			</if>
		</where>
	</sql>
	<sql id="orderBy">
		ORDER BY 
		<if test="sort != null and sort != ''">
			${sort} ${order},
		</if>
		T.SEQ,T.CREATE_DATE DESC 
	</sql>
	
	<!-- 根据ID查询单条数据 -->
	<select id="findOne" resultType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		SELECT
		<include refid="tableColumns" />
		FROM ES_CATEGORY T
		WHERE T.ID = #{id}
	</select>
	
	<!-- 查询数据集合 -->
	<select id="findList" resultType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		select * from(select T.*,rownum rn from(
			SELECT
			<include refid="tableColumns"/>
			FROM ES_CATEGORY T
			<include refid="whereStr"/>
			<include refid="orderBy"/>
		) T WHERE rownum &lt; = #{page}*#{rows}) where rn &gt; (#{page}-1)*#{rows}
	</select>
	
	<!-- 查询所有数据集合 -->
	<select id="findAll" resultType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		SELECT
		<include refid="tableColumns"/>
		FROM ES_CATEGORY T
		<include refid="whereStr"/>
		<include refid="orderBy"/>
	</select>
	
	<!-- 查询数据总数 -->
	<select id="findTotal" resultType="Integer">
		SELECT
		COUNT(1) total
		FROM ES_CATEGORY T
		<include refid="whereStr"/>
	</select>
	
	<!-- 查询数据总数排除当前数据 -->
	<select id="findExTotal" resultType="Integer">
		SELECT
		COUNT(1) total
		FROM ES_CATEGORY T
		<include refid="whereStr"/>
		AND T.ID != #{id}
	</select>
	
	<!-- 数据新增 -->
	<insert id="save" parameterType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		INSERT INTO ES_CATEGORY (
			
			
			
			
			
			REMARKS,
			
			NAME,
			KEY_WORDS,
			DESCRIBE,
			SEQ,
			PARENT_ID,
			MEASURE_UNIT,
			GRADE,
			IS_SHOW,
			SHOP_ID,
			CREATE_USER,
			CREATE_DATE,
			ID
		)
		VALUES (
			
			
			
			
			
			#{remarks,jdbcType=VARCHAR},
			
			#{name,jdbcType=VARCHAR},
			#{keyWords,jdbcType=VARCHAR},
			#{describe,jdbcType=VARCHAR},
			#{seq,jdbcType=NUMERIC},
			#{parentId,jdbcType=VARCHAR},
			#{measureUnit,jdbcType=VARCHAR},
			#{grade,jdbcType=NUMERIC},
			#{isShow,jdbcType=VARCHAR},
			#{shopId,jdbcType=VARCHAR},
			#{createUser.userName},
			sysdate,
			#{id}
		)
	</insert>
	
	<!-- 数据修改 -->
	<update id="update" parameterType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		UPDATE ES_CATEGORY
		<set>
			
			
			
			
			
			<if test="remarks !=null and remarks !=''">
			REMARKS=#{remarks},
			</if>
			
			<if test="name !=null and name !=''">
			NAME=#{name},
			</if>
			<if test="keyWords !=null and keyWords !=''">
			KEY_WORDS=#{keyWords},
			</if>
			<if test="describe !=null and describe !=''">
			DESCRIBE=#{describe},
			</if>
			<if test="seq !=null and seq !=''">
			SEQ=#{seq},
			</if>
			<if test="parentId !=null and parentId !=''">
			PARENT_ID=#{parentId},
			</if>
			<if test="measureUnit !=null and measureUnit !=''">
			MEASURE_UNIT=#{measureUnit},
			</if>
			<if test="grade !=null and grade !=''">
			GRADE=#{grade},
			</if>
			<if test="isShow !=null and isShow !=''">
			IS_SHOW=#{isShow},
			</if>
			<if test="shopId !=null and shopId !=''">
			SHOP_ID=#{shopId},
			</if>
			UPDATE_USER = #{updateUser.userName},
			UPDATE_DATE = sysdate,
			ID = #{id}
		</set>
		WHERE ID = #{id}
	</update>
	
	<!-- 数据删除 -->
	<delete id="delete">
		DELETE FROM ES_CATEGORY WHERE id IN 
		<foreach item="listId" collection="list"
			open="(" separator="," close=")">
			#{listId}
		</foreach>
	</delete>
	
	<!-- 数据假删除 -->
	<update id="remove" parameterType="com.seeyoui.kensite.framework.eshop.category.domain.Cate">
		UPDATE ES_CATEGORY
		<set>
			UPDATE_USER = #{updateUser.userName},
			UPDATE_DATE = sysdate,
			DEL_FLAG = '1'
		</set>
		WHERE ID = #{id}
	</update>
	
</mapper>